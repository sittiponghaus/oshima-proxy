PORT=3000

# Optional. Default property fetch uses non-browser UA `oshima-proxy/1.0` and needs no cookie.
# Only set these if you override OSHIMA_USER_AGENT to a real browser UA (then CF requires cf_clearance).
# OSHIMA_COOKIE=
# OSHIMA_USER_AGENT=
