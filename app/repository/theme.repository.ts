/**
 * Theme preference persistence + document / map-style side effects.
 */
export {
  applyThemeToDocument,
  mapStyleForTheme,
  resolveTheme,
  themeAtom,
  themeAtomKey,
  type Theme
} from "@/app/store/theme.store"
