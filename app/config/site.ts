/** Public source repository for this viewer (edit if the remote differs). */
export const GITHUB_REPO_URL = "https://github.com/sittiponghaus/oshima-proxy"

/** Official Oshimaland English site (canonical public homepage). */
export const OSHIMALAND_SITE_URL = "https://www.oshimaland.com/"

/** Document `<title>` for the CSR shell. */
export const PAGE_TITLE = "OL Proxy"

/**
 * Document meta description — clearer map UX over public Oshimaland reports.
 * Keep in sync with `app/entrypoint.html`.
 */
export const PAGE_DESCRIPTION =
  "OL Proxy — a clearer map for Oshimaland public incident reports (deaths, fires, and related events) on OpenFreeMap."
